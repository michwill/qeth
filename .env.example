# Copy to .env (gitignored) for local test config.
#
# A local mainnet *archive* node makes the anvil integration tests
# (tests/test_live_anvil.py) fork instantly with no public-RPC rate limits.
# Falls back to a public RPC if unset.
QETH_ANVIL_FORK_RPC=http://192.168.1.10:8545
